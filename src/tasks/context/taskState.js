import React,{useState} from React;
import TaskContext from "./TaskContext";

//=> let's Make Fetch Calls

const TaskCalls = () => {

    const host = 'http://localhost:9000'


    //--> GET NOTES
    const getTask = aync () => {
        const res = await fetch
    }
    
}